# Focus Guard 要件定義書

## 現行仕様（0.2・使用アプリ制御の撤去）

この節を以下の旧仕様より優先する。

- 複数の作業を中断状態で保持し、別タスクへ切り替えられる。計時中の作業は常に1件とする。
- 再開時は同じセッションIDと累積時間を引き継ぎ、中断時間は加算しない。
- 中断中の作業は再起動後も保持し、個別に再開・終了できる。中断中のタスクの完了・削除は作業終了後に行う。

- タスクは複数タグを持ち、登録・変更・解除ができる。既存のタグなしタスクも引き継ぐ。
- タグ別の一覧絞り込み、タスク名・メモ・タグの検索を提供する。
- 追加・編集・削除・作業操作・出力はアイコンとツールチップで表現する。
- 標準タイトルバーを非表示にし、移動用領域と最小化・最大化・終了ボタンを提供する。

- 使用アプリの指定・追加・監視・制限・脱線検知は撤去する。監視用プロセスと再接続処理は起動しない。
- タスクを選ぶだけで作業を開始できる。
- 各ディスプレイの右下にタスク名と経過時間を表示する。赤枠は付けない。
- 右下表示はクリック可能とし、クリックするとメイン画面を表示・最小化解除・前面化する。
- 一時停止・再開・終了・右下表示の非表示切替を提供する。一時停止中は計時と右下表示を停止する。
- タスク管理、作業履歴、JSONL出力、ローカル保存は継続する。
- 既存のタスク・履歴を引き継ぐ。旧版の未処理の脱線検出は移行時に記録済みにし、入力画面は開かない。

以下は初期設計の履歴であり、アプリ制御およびクリック透過に関する記述は現行版の対象外。

---

**版:** 0.1  
**作成日:** 2026-09-08  
**対象:** Windows 10/11 デスクトップアプリ  
**開発方針:** 仕様駆動開発・ローカルファースト

## 1. 背景と目的

ADHD傾向のあるユーザーが、作業中に複数のアプリや情報へ注意を奪われることを防ぎ、ひとつのタスクに戻りやすくする。

本アプリはタスク管理ツールではなく、作業開始時に「今やること」と「使ってよいアプリ」を限定し、脱線を検知・記録する集中支援ツールである。

## 2. 対象ユーザー

- Windows PCで仕事・勉強を行う個人
- タスクの切り替えや通知で集中が途切れやすい人
- 作業ログを後から振り返りたい人
- 将来的にAIで作業傾向を分析したい人

医療上の診断・治療・改善を目的とするアプリではない。

## 3. プロダクト方針

- 作業中の選択肢を減らす
- 脱線を責めず、理由を記録して改善につなげる
- 中断・解除・アプリ追加などの逃げ道を用意する
- 初期版ではAI連携を行わず、AI入力可能なデータを蓄積する
- データは外部へ送信せず、ユーザーのPC内に保存する

## 4. スコープ

### 4.1 MVPに含めるもの

- タスクの作成・編集・削除・完了
- タスクを選択した作業セッションの開始
- 使用を許可するアプリの登録
- Windows前面アプリの監視
- 許可外アプリを開いた際の脱線記録
- 脱線理由のカテゴリ選択と自由記述
- 作業の一時停止・再開・終了
- 作業中の常駐オーバーレイ
- JSONL形式でのイベント出力
- ローカルデータ保存

### 4.2 将来対応するもの

- 許可外アプリの自動最小化・許可アプリへの復帰
- Windowsログイン時の自動起動
- タスクトレイ常駐
- キーボードショートカット
- ブラウザのサイト単位制限
- 作業時間・休憩時間の設定
- 日次・週次の傾向分析
- AIによる分析・提案

### 4.3 対象外

- 医療診断、治療、服薬管理
- クラウド同期
- アカウント登録・ソーシャル機能
- 初期版でのAI API接続
- パスワード入力やセキュリティソフトを含むOS全体の完全ロック

## 5. 利用フロー

### 5.1 作業開始

1. ユーザーがタスク一覧からタスクを選ぶ
2. 使用を許可するアプリのプロセス名を入力する
3. 作業開始を押す
4. セッションを開始する
5. 全ディスプレイの右下に小さな常駐表示を出す（画面の赤枠は表示しない）
6. 許可アプリの監視を開始する

### 5.2 作業中

- 画面右下に作業名/経過時間を表示（非表示切替可能）
- 前面アプリが許可外の場合、脱線入力画面を表示する
- ユーザーは理由カテゴリとメモを記録する
- ユーザーは作業を一時停止、再開、終了できる
- ユーザーは必要なアプリを作業中に追加できる

### 5.3 作業終了

1. ユーザーが作業終了を押す
2. セッション終了時刻を記録する
3. 作業名表示を終了
4. セッション完了イベントを保存する

## 6. 機能要件

### FR-001 タスク管理

システムはタスクを作成、編集、削除、完了できなければならない。

タスクは少なくとも以下を持つ。

- タスクID
- タスク名
- メモ
- 完了状態
- 作成日時
- 更新日時
- 完了日時（完了時）

### FR-002 セッション開始

システムは1つのタスクと許可アプリ一覧を指定してセッションを開始できなければならない。

### FR-003 アプリ監視

システムはWindowsの前面ウィンドウに対応するプロセス名を定期的に取得し、許可アプリ一覧と照合しなければならない。

### FR-004 脱線記録

許可外アプリを検出した場合、システムは以下を記録できなければならない。

- 検出時刻
- タスクID
- セッションID
- 開いたアプリ
- 理由カテゴリ
- 自由記述
- 脱線時間
- 対応結果

### FR-005 常駐オーバーレイ

作業中、システムは全ディスプレイの右下に以下を小さく表示しなければならない。画面の赤枠は不要。

- タスク名
- 経過時間

オーバーレイは通常のマウス操作を妨げてはならない。

### FR-006 セッション制御

システムはセッションの一時停止、再開、終了を提供しなければならない。

### FR-007 アプリ追加

作業中、ユーザーは許可アプリを追加できなければならない。追加イベントは記録する。

### FR-008 データ出力

システムはイベントログをJSONL形式でローカルファイルに出力できなければならない。

## 7. データ要件

### 7.1 保存方式

- アプリ内部データ：ローカルファイルまたはSQLite
- イベントログ：JSONL
- 外部サーバーへの送信：なし
- 日時：ISO 8601形式、タイムゾーン付き
- 経過時間：秒単位の整数

### 7.2 イベント種別

- `session_started`
- `session_paused`
- `session_resumed`
- `session_completed`
- `distraction`
- `app_added`
- `task_completed`

### 7.3 脱線イベント例

```json
{
  "event_type": "distraction",
  "session_id": "session_001",
  "task_id": "task_001",
  "timestamp": "2026-09-08T14:32:10+09:00",
  "opened_app": "chrome",
  "reason_category": "難しくて詰まった",
  "reason_text": "次に何をすればよいか分からなくなった",
  "duration_seconds": 185,
  "action": "recorded"
}
```

## 8. 非機能要件

### NFR-001 対応環境

Windows 10/11で動作すること。

### NFR-002 プライバシー

作業ログとアプリ名はローカルに保存し、ユーザーの明示的な操作なしに外部送信しないこと。

### NFR-003 可用性

監視処理が一時的に失敗しても、タスク・セッション・イベントログを失わないこと。

### NFR-004 操作性

脱線理由の記録は、カテゴリ選択と短い自由記述で完了できること。入力を強制しすぎないこと。

### NFR-005 安全性

初期版ではOS全体を完全にロックしないこと。ユーザーがいつでも作業を終了できること。

## 9. 受入条件

- タスクを追加して一覧に表示できる
- タスクを編集・削除・完了できる
- タスクと許可アプリを指定して作業を開始できる
- 作業開始後、各ディスプレイの右下にタスク名と経過時間が表示される
- 画面の赤枠は表示されず、右下の表示はマウス操作を妨げない
- 許可外アプリの検出時に脱線記録画面が表示される
- 理由カテゴリとメモを保存できる
- 作業を一時停止・再開・終了できる
- 作業中に許可アプリを追加できる
- 作業終了時にオーバーレイが消える
- JSONLファイルを出力できる
- 出力データにセッションID、タスクID、イベント時刻が含まれる
- アプリ終了後もタスクとログが保持される

## 10. リスクと未決事項

| 項目 | 内容 | 対応方針 |
|---|---|---|
| 完全なアプリ遮断 | Windowsの権限・セキュリティ上、完全遮断は難しい | MVPは検知・記録、厳格モードは別設計 |
| ブラウザ制御 | ブラウザ単位ではサイトまで制限できない | 将来サイト単位制限を検討 |
| 複数モニター | ディスプレイごとにオーバーレイが必要 | MVPで全ディスプレイを対象にする |
| ログの個人情報 | アプリ名や自由記述に機密情報が含まれる可能性 | ローカル保存、将来匿名化機能を検討 |
| Electron依存 | 配布時のサイズ・脆弱性管理が必要 | 定期的に依存関係を更新 |

## 11. 現在の実装状態

MVPとして、タスク管理、セッション制御、前面アプリ監視、脱線記録、常駐オーバーレイ、JSONL出力を実装済み。厳格なアプリ遮断、タスクトレイ、AI連携は未実装。
